// Hello_world.cpp file
#include <iostream>// ใช้สำหรับการแสดงผล
using namespace std;// ใช้สำหรับการแสดงผล
int main() //ฟังก์ชันหลักของโปรแกรม
{// ฟังก์ชันหลักของโปรแกรมเริ่มที่นี่
    std::cout << "Hello world\n";// ใช้เพื่อแสดงข้อความไปยังหน้าจอ ในCodeนี้แสดงข้อความ "Hello world" บนหน้าจอ
    return 0;// คืนค่า 0 เพื่อบอกว่าโปรแกรมทำงานสำเร็จ 0 หมายถึงสำเร็จ และค่าที่ไม่ใช่ 0 หมายถึงเกิดข้อผิดพลาด
}// ฟังก์ชันหลักของโปรแกรมจบที่นี่
